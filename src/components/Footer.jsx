import { footerText } from '../data/siteData';

const Footer = () => {
    return (
        <footer>
            <div>
                {footerText}
            </div>
        </footer>
    );
};

export default Footer;
